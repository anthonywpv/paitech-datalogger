<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Solo la variante debug puede hablar por HTTP con Django ejecutándose en
      la computadora anfitriona. La variante release conserva la política del
      manifest principal: tráfico en texto claro deshabilitado.
    -->
    <application android:networkSecurityConfig="@xml/debug_network_security_config" />

</manifest>
